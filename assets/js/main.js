/**
 * Gautam Kumar Maurya — Main JS Dispatcher
 * Loads modern app.js logic cleanly.
 */

// Import app logic if in ES module environment or include execution check
if (typeof window !== 'undefined') {
  console.log("Gautam Kumar Maurya Portfolio (2026) — App Loaded");
}
